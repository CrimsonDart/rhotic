#+title: Ex
#+PROPERTY: header-args :tangle ex.rs
#+auto_tangle: t




I wrote the code this way since thats the only way i can get text highlighting in emacs :)

#+begin_src rust
pub mod cs {
    vulkano_shaders::shader!{
    ty: "compute",
    src: r"
#+end_src

#+begin_src glsl
#version 460

layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

layout(set = 0, binding = 0) buffer Data {
    uint data[];
} buf;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    buf.data[idx] *= 12;
}


#+end_src

#+begin_src rust
",}}
#+end_src
